# Google Gemini API Key (required for DSPy optimization - matches your Chrome extension)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: OpenAI API Key (fallback for optimization if Gemini not supported in DSPy)
# OPENAI_API_KEY=your_openai_api_key_here

# Optional: Custom database path
# DATABASE_PATH=/path/to/custom/database.db

# Optional: Custom port (defaults to 7532)
# PORT=7532

# Optional: Environment
# ENVIRONMENT=development